// Soplang Control Flow Examples
// This example shows loops, conditions, and error handling in Soplang

// ----- Conditional (haddii/haddii_kale/haddii_kalena) -----
tiro score = 85

qor("Checking score status:")
haddii (score >= 90) {
    qor("Grade: A - Excellent!")
} haddii_kale (score >= 80) {
    qor("Grade: B - Very Good!")
} haddii_kale (score >= 70) {
    qor("Grade: C - Good")
} haddii_kale (score >= 60) {
    qor("Grade: D - Pass")
} haddii_kalena {
    qor("Grade: F - Failed")
}

// ----- For Loop (ku_celi) -----
qor("\nCounting from 1 to 10:")
ku_celi i min 1 ilaa 10 {
    qor("Number: " + qoraal(i))
}

// ----- Loop with Break (jooji) -----
qor("\nCounting until finding a multiple of 7:")
ku_celi j min 1 ilaa 20 {
    qor("Checking: " + qoraal(j))
    
    haddii (j % 7 == 0) {
        qor("Found multiple of 7: " + qoraal(j))
        jooji
    }
}

// ----- Error Handling with Try-Catch -----
qor("\nError handling examples:")

// Example 1: Division by zero
isku_day {
    door result = 10 / 0
    qor("This won't be printed")
} qabo (error) {
    qor("Division error caught: " + error)
}

// Example 2: Type error
isku_day {
    tiro num = tiro("not a number")
    qor("This won't be printed either")
} qabo (error) {
    qor("Type error caught: " + error)
}

// ----- Nested Conditionals -----
tiro x = 15
tiro y = 10

qor("\nNested conditionals:")
haddii (x > 10) {
    qor("x is greater than 10")
    
    haddii (y > 5) {
        qor("And y is greater than 5")
    } haddii_kalena {
        qor("But y is not greater than 5")
    }
} haddii_kalena {
    qor("x is not greater than 10")
}

// ----- Loop with Continue (sii_wad) -----
qor("\nSkipping even numbers:")
ku_celi k min 1 ilaa 10 {
    haddii (k % 2 == 0) {
        sii_wad  // Skip even numbers
    }
    qor("Odd number: " + qoraal(k))
} 